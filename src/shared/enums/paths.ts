export namespace Paths {
  export enum Primary {
    LAYOUT = '/',
    LOGIN = 'login',
    NOTFOUND = '*'
  }
  export enum Layout {
    DASHBOARD = '',
    USER = 'user',
    GROUP = 'group',
    TEST = 'test',
  }
}
