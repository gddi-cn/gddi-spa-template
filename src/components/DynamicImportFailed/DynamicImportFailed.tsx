import React from 'react'
import { Typography } from '@mui/material'

const DynamicImportFailed: React.FC = () => {
  return (
    <Typography variant={'body1'}>Failed to load component...</Typography>
  )
}
export default DynamicImportFailed
